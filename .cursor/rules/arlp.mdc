---
description: 
globs: 
alwaysApply: true
---

**"A Life Is A Random Journey"**

> Un RPG de texto donde no eliges quién eres. Solo vives lo que te toca.

---

## 🧠 CONCEPTO GENERAL

Este es un **RPG de texto único e inmersivo**, donde cada partida es una vida diferente. No eliges tu nombre, género, clase social ni destino inicial. Solo recibes lo que el mundo decide darte… y decides qué hacer con ello.

Es como si estuvieras viviendo una novela interactiva que cambia completamente en cada partida, pero con una **interfaz moderna, atractiva, de alto rendimiento y completamente responsiva**, diseñada para **aprovechar al máximo las pantallas de alta gama de dispositivos Android**, acompañada de **audio de alta calidad** (música y efectos) para una inmersión profunda.

### **Puntos Clave del Enfoque:**
* **Narrativa Dinámica:** Construida sobre **personajes no jugadores (NPCs) fijos pero con encuentros condicionales** y **arcos de historia específicos basados en la localización o trayectoria de vida del jugador**, lo que permite una rejugabilidad exponencial y una sensación de vivir una vida verdaderamente única.
* **Experiencia Offline Total:** Toda la lógica del juego y la persistencia de datos residirán de forma segura y autónoma en el propio dispositivo Android del usuario, garantizando una experiencia de juego completamente offline.
* **Generación Procedural Ligera:** Si bien la historia principal y los arcos de localización son predefinidos, se explorará el uso de plantillas de texto y combinación de elementos (adjetivos, sustantivos, verbos aleatorios) para generar descripciones y eventos menores de forma procedural. Esto aumentará aún más la variedad y la sensación de una vida verdaderamente única, sin requerir la creación manual de cada micro-evento.

---

## 🌍 AMBIENTACIÓN

Un mundo de fantasía realista, donde:
* Existen magia, dragones, elfos y reinos.
* Pero también hay pobreza, enfermedades, traiciones, accidentes y muertes sin gloria.
* No todo tiene sentido. A veces, simplemente pasa algo y ya.
* Las decisiones importan, pero no siempre obtienes un final feliz.

---

## 🧬 LO QUE HACE TU JUEGO ÚNICO

Tu juego no sigue una historia lineal. Es una **experiencia personalizada y profundamente emocional**. Cada jugador vive una vida distinta, sin las limitaciones de un personaje predefinido. Y eso lo hace rejugable, impredecible y auténtico.

---

## 📋 PLAN GENERAL DEL JUEGO

### 1. **Sin elección inicial**
* El juego genera automáticamente:
    * Tu nombre
    * Tu género
    * Tu lugar de nacimiento
    * Tu familia
    * Tu evento inicial (aleatorio: ataque, secuestro, paz, oportunidad...)

### 2. **Vida ramificada y Arcos de Localización**
* Cada decisión te lleva por un camino distinto.
* **La historia se desarrolla en "arcos" o "capítulos" basados en la ubicación y las decisiones del jugador.** Por ejemplo:
    * Si entras en la academia, la narrativa se centra en la vida académica, los profesores, rivalidades y eventos dentro de la academia.
    * Si terminas en el palacio, la trama gira en torno a intrigas cortesanas, reyes, nobles y misiones diplomáticas/militares dentro de ese entorno.
    * Lo mismo para un gremio de ladrones, una granja aislada, un campamento de mercenarios, etc.
* Puedes ser:
    * Un héroe legendario
    * Un villano temido
    * Un campesino común
    * Un rey amado o odiado
    * Un asesino arrepentido
    * Un bandido sin remordimientos
    * O incluso alguien que nunca logra nada importante

### 3. **Personajes No Jugadores (NPCs) Condicionales**
* **NPCs Fijos:** Habrá un conjunto de NPCs con nombres, personalidades e historias de fondo predefinidas.
* **Encuentros Dinámicos:** Que conozcas o no a un NPC dependerá completamente del camino que tome tu vida.
    * Si tu vida te lleva a la Academia, es posible que conozcas al "Maestro Elara". Si tu vida te lleva al Palacio, nunca la conocerás.
    * Un NPC puede morir sin haberte conocido si los eventos de su propia vida o de la historia general así lo dictan.
    * Tus interacciones con un NPC (o la falta de ellas) afectarán su destino y el tuyo.
* **Relaciones con NPCs:** Sistema de afinidad/reputación con los NPCs que afectará sus reacciones y las opciones disponibles.

### 4. **Fantasía realista**
* Magia, dragones y criaturas existen...
* Pero también hay hambre, corrupción, miedo, vejez y muerte natural.
* Todo puede fallar: tu arma puede romperse, tu comida puede estar podrida, tu mente puede engañarte.

### 5. **Sistema de tiempo y edad**
* Envejeces naturalmente.
* Puedes morir joven por una decisión tonta.
* O llegar a viejo, preguntándote si valió la pena todo lo hecho.

### 6. **Diálogos con peso emocional y Dilemas Morales**
* Tus palabras afectan relaciones. Algunas frases pueden abrir nuevas rutas, otras cerrarlas para siempre. Incluso puedes elegir no hablar… y eso también tiene consecuencias.
* El juego presentará situaciones donde no hay una "elección buena" o "mala" clara. Las decisiones afectarán un sistema de valores internos o "salud mental/moral" oculta del personaje, que influirá sutilmente en futuras reacciones, opciones de diálogo o incluso desenlaces de vida a largo plazo, añadiendo capas de complejidad emocional.

### 7. **Mundo reactivo**
* Tus acciones tienen consecuencias permanentes.
* Personajes recuerdan lo que hiciste.
* El mundo cambia según tus actos, aunque nadie más lo note.

### 8. **Finales únicos y Legado**
* No hay un solo final. Hay docenas. Tu final depende de qué hiciste, cómo moriste, cómo te recordaron y si dejaste legado o fuiste olvidado.
* **Ecos del Pasado:** Tu vida no termina con tu muerte. Dependiendo de cómo viviste y tu legado (bueno o malo), el mundo puede recordar a tu personaje. Esto podría manifestarse en futuras partidas, donde un nuevo personaje generado pueda encontrar monumentos, ruinas, historias orales o incluso NPCs descendientes de aquellos con los que interactuó tu vida anterior, con sus propias opiniones basadas en tus acciones pasadas.

### 9. **Reincorporación con memoria parcial**
* Si mueres, puedes "reiniciar".
* Pero no recuerdas todo.
* Solo sabes que has vivido antes… y que quizás esta vez puedas cambiar algo.

### 10. **Sistema de objetos defectuosos**
* Armas que fallan
* Comida en mal estado
* Pociones que curan… o envenenan
* Objetos con pasado propio

---

## 🧩 ¿QUÉ TIPO DE VIDAS PUEDEN OCURRIR?

| Tipo de vida | Descripción |
| :----------- | :---------- |
| **Normal** | Viviste y moriste sin hacer nada memorable |
| **Aventurera** | Viajaste, descubriste cosas, tal vez salvaste al mundo |
| **Noble** | Subiste de posición, gobernaste, hiciste leyes |
| **Oscura** | Tomaste caminos prohibidos, mataste, mentiste, sobreviviste |
| **Redimida** | Cometiste errores, pero al final intentaste enmendarte |
| **Trágica** | Tenías potencial, pero caíste por errores, traiciones o mala suerte |
| **Legendaria** | Tu nombre se canta en todas partes, incluso después de muerto |

---

## 🪙 NO HAY UN CAMINO CORRECTO

El juego no premia la bondad, ni castiga la maldad. Simplemente te da libertad. Lo demás depende de ti.

---

## 🌟 FRASE FINAL PARA EL PLAN

> “Este no es un juego sobre salvar al mundo. Es un juego sobre vivir una vida. Una vida que no elegiste. Pero que tú defines.”

---

## 🛠️ TECNOLOGÍAS Y ARQUITECTURA (Con Énfasis en Rendimiento, UI Moderna, Responsividad y Lógica Compleja de NPCs/Arcos - **Todo Local en Android**)

### Tecnologías Clave:

- **Motor del Juego (Backend/Lógica - Python - **¡Local en el Dispositivo Android!**):**
    - **Python:** Lenguaje principal para toda la lógica procedural, narrativa, simulación de mundo y generación de eventos.
    - **¡Ejecución Local!:** El intérprete de Python y el código del backend serán **empaquetados dentro de la aplicación Android**.
        - Se utilizará **Chaquopy** o soluciones similares para integrar un intérprete Python funcional dentro del APK/AAB.
    - **Librerías Python:**
        - **`numpy`:** Para cualquier cálculo numérico complejo si fuera necesario.
        - **`json`:** Para la gestión de nodos narrativos y datos del juego.
        - **`random` / `secrets`:** Para generación aleatoria segura de eventos y personajes.
        - **Librerías de ORM/Base de Datos (ej. SQLAlchemy con SQLite):** **CRÍTICO** para una gestión robusta y compleja de NPCs, sus estados, relaciones y la persistencia de la historia global del mundo, todo guardado en un archivo `.db` local en el dispositivo.
    - **Comunicación Interna:** Aunque sigue un modelo de API REST conceptual, la comunicación entre el frontend de React Native y el backend de Python será a través de mecanismos internos proporcionados por la solución de integración Python (ej. llamadas a funciones Python expuestas a través de un puente nativo). Esto garantiza la funcionalidad offline.

- **Interfaz de Usuario (Frontend Móvil Android):**
    - **React Native + Expo (con TypeScript): rally integrado** Framework principal para construir la aplicación móvil Android. Ofrece una UI nativa, buen rendimiento y un ecosistema maduro para desarrollo móvil.
    - **UI Frameworks:**
        - **`react-native-paper`:** Componentes Material Design, modernos y limpios para una UI atractiva y funcional.
        - **`NativeBase`:** Para un diseño adaptable y componentes pre-construidos que aceleran el desarrollo.
        - **Custom Components:** Creación de componentes personalizados para elementos únicos del juego, manteniendo la consistencia visual y el rendimiento.
    - **Animaciones:** `Animated` API de React Native o `react-native-reanimated` para transiciones suaves, efectos de escritura de texto y animaciones de sprites (si los hubiera).

- **Audio de Alta Calidad en Frontend (React Native):**
    - **`expo-av`:** Para reproducir música de fondo y efectos de sonido. Soporta varios formatos de audio y ofrece control sobre volumen, loops, etc.
    - **Archivos de audio:** Uso de archivos WAV, MP3 u OGG de **alta calidad (ej. 44.1kHz, 16-bit estéreo)** para música y efectos. La "calidad" del audio la define el asset de audio proporcionado.

- **Persistencia de Datos (Frontend):**
    - **`expo-secure-store`:** Para almacenamiento seguro y persistente de datos sensibles (estado del juego, progreso del jugador, memoria de vidas anteriores) si se necesitan fuera de la DB de Python.
    - **`AsyncStorage` (React Native):** Para datos no sensibles que necesiten persistencia rápida.

- **Gestión de Recursos (Frontend):**
    - **Sistema de imágenes:** Manejo eficiente de imágenes (PNG, JPG, WebP para optimización de tamaño) que se mostrarán en la UI. Carga y caché inteligente.
    - **Fuentes personalizadas:** Carga de fuentes para la estética del texto narrativo.

- **Distribución:**
    - **EAS Build (Expo Application Services):** Para exportar la aplicación final en Android (APK/AAB). Maneja la firma y la compilación.

### Arquitectura General:

```mermaid
graph TD
    subgraph Android Device
        A[Interfaz React Native (Mobile App)]
        B(Motor Lógico Python - Backend)
        C[Generador Aleatorio]
        D[Sistema de Ramificaciones]
        E[Sistema de Tiempo y Edad]
        F[Sistema de Eventos Únicos]
        G[react-native-paper / NativeBase / Custom UI]
        H[expo-secure-store / AsyncStorage]
        I[Guardado de Partida]
        J[expo-av (Música y SFX)]
        K[Gestión de Imágenes Dinámicas]
        L[Base de Datos de Nodos JSON (Data Local)]
        M[Base de Datos de NPCs - SQLite/SQLAlchemy (Data Local)]
        N[Gestor de Arcos de Historia/Localización]

        A -- Internas/Puente Nativo --> B
        B -- Lee/Escribe --> M
        B -- Lee --> L
        B --> C
        B --> D
        B --> E
        B --> F
        A --> G
        A --> H
        H --> I
        A --> J
        A --> K
        B --> N
    end
```

### Flujo de Datos y Comunicación:

1.  **Inicio de Juego:** El frontend solicita al backend Python (a través del puente nativo o conexión local) que inicie una nueva partida.
2.  **Backend Python:** Genera el personaje inicial y el primer nodo narrativo. **Aquí, el gestor de arcos de historia y la base de datos de NPCs (ambos locales) entran en juego para determinar el punto de partida y los NPCs relevantes.** Guarda el estado en su DB local, y responde con el JSON del nodo.
3.  **Frontend React Native:** Recibe JSON, actualiza la UI (texto, opciones, imagen), reproduce música/SFX asociados.
4.  **Decisión del Jugador:** Jugador selecciona una opción en la UI.
5.  **Frontend React Native:** Envía la decisión al Backend Python.
6.  **Backend Python:** Procesa la decisión:
    * Actualiza el estado del personaje y del mundo en la base de datos SQLite local.
    * **Consulta la base de datos de NPCs local para ver si un NPC relevante debería aparecer o si su estado cambia (ej. muere, se une a tu facción).**
    * **El Gestor de Arcos de Historia determina el siguiente "capítulo" o "localización" de la vida del personaje, influyendo qué nodos se cargan desde los datos JSON locales.**
    * Determina el siguiente nodo basándose en la lógica ramificada, los eventos aleatorios y las interacciones con NPCs.
    * Responde con el nuevo JSON del nodo.
7.  **Ciclo continúa.**
8.  **Guardado/Carga:** El backend Python maneja todo el guardado y carga de la partida directamente en el archivo `game.db` SQLite local, accesible por el backend.

---

## 🎨 ESPECIFICACIONES DE DISEÑO DE UI (Pantallas, Tipografía y Responsividad en Pantallas Modernas)

El diseño de UI debe ser **extremadamente responsivo**, adaptándose sin problemas a la gran variedad de tamaños, relaciones de aspecto (especialmente las más altas y estrechas) y densidades de píxeles de los dispositivos modernos. Se utilizarán unidades relativas (`%`, `vw`, `vh`) y utilidades de escalado avanzado (como `react-native-size-matters` o `react-native-responsive-fontsize`) para asegurar una experiencia visual y de legibilidad óptima.

### 1. **Resoluciones de Pantalla Objetivo (Basadas en densidades de píxeles modernas):**
* **Base de Diseño (referencia):**
    * **Ancho:** `360 dp` (equivalente a un ancho de pantalla de un dispositivo MDPI, pero que se escalará a resoluciones como `1080px` o `1440px` en dispositivos XXHDPI/XXXHDPI).
    * **Alto:** `760 dp` a `890 dp` (representando relaciones de aspecto modernas como 19:9, 20:9 o 21:9).
* **Densidades de Píxeles Prioritarias:**
    * **xhdpi (2x):** Aproximadamente 320 dpi.
    * **xxhdpi (3x):** Aproximadamente 480 dpi.
    * **xxxhdpi (4x):** Aproximadamente 640 dpi.
    * **Objetivo:** Diseñar con una base de `dp` y confiar en que React Native y los assets en `@2x`, `@3x`, `@4x` manejarán la densidad física.
* **Soporte Extendido:** La UI debe escalar grácilmente a tablets Android (`min-width: 600dp` y `min-width: 720dp` en landscape), aprovechando el espacio adicional con un layout de dos columnas o reorganización de elementos.

### 2. **Dimensiones de Elementos (basadas en `dp` y porcentajes del viewport):**
* **Márgenes y Relleno (Padding):**
    * **Espacio Mínimo (XS):** `8dp` (botones pequeños, espaciado entre ítems, íconos).
    * **Espacio Normal (SM):** `16dp` (espaciado de texto, padding de contenedores, márgenes internos).
    * **Espacio Medio (MD):** `24dp` (márgenes de pantalla, secciones, espaciado entre bloques de contenido).
    * **Espacio Grande (LG):** `32dp` a `48dp` (grandes separaciones entre elementos clave o grupos de contenido).
* **Altura de Botones de Opción:**
    * **Vertical:** `48dp` a `64dp` para asegurar un objetivo táctil cómodo y espacio para texto multilinea si es necesario.
    * **Horizontal:** `40dp` a `56dp` (en barras de navegación o herramientas).
* **Área de Contenido Principal (Texto Narrativo):**
    * Ocupará entre el `60%` y el `70%` del **alto disponible** de la pantalla y entre el `85%` y el `95%` del **ancho disponible**.
    * Se garantizará que el texto sea visible y legible sin superponerse a otros elementos clave (como la imagen o las opciones).
    * **Max-Width:** Para pantallas muy anchas (tablets en landscape), el texto principal y las opciones no deberían estirarse indefinidamente, sino tener un `max-width` para mantener la legibilidad (ej. `400dp` a `600dp`), centrando el contenido.
* **Área de Imagen:**
    * `25%` a `35%` del **alto de la pantalla**, posicionada típicamente en la parte superior del área de contenido.
    * **Completamente responsiva:** `width: '100%'` (del contenedor) y `height: 'auto'`, manteniendo la relación de aspecto original de la imagen.
    * Las imágenes pueden tener un `max-height` para evitar que ocupen demasiado espacio vertical en pantallas más cortas.

### 3. **Resoluciones de Imágenes y Optimización:**
* **Imágenes de Escenas/Contexto:**
    * **Resolución Base:** `1080px` (ancho) para la referencia `3x` de una pantalla moderna (ej. `1080px` para un ancho de 360dp en `xxxhdpi`).
    * **Variantes de Densidad:** Se proporcionarán assets en `@2x`, `@3x`, y idealmente `@4x` si el diseño lo requiere, para cubrir todas las densidades de píxeles.
    * **Formato:** Preferiblemente `WebP` (para mejor compresión y calidad), `PNG` (para transparencias de alta calidad) o `JPG` (para fotografías). Optimizar el tamaño de archivo es crucial.
* **Iconos de UI:** `48dp` a `64dp` en `SVG` (para escalado perfecto y menor tamaño de archivo) o `PNG` con `@2x`, `@3x`, `@4x` variantes.

### 4. **Especificaciones de Tipografía (con `react-native-responsive-fontsize` para escalado):**
* **Fuentes Personalizadas:**
    * Se cargarán al inicio de la aplicación usando `expo-font`.
    * **Fuente Narrativa Principal (ej. `Lora`, `Merriweather`, `Spectral`):** Para el cuerpo principal del texto. Estilo legible, ligeramente serif o un sans-serif elegante, que evoque una sensación de "lectura de novela".
    * **Fuente para Títulos/Énfasis (ej. `Cinzel Decorative`, `Oswald`, `Montserrat`):** Para títulos de nodos, nombres de capítulos o elementos importantes. Clara, impactante y que contraste bien con la narrativa.
    * **Fuente de UI (ej. `Roboto`, `Inter`, `Open Sans`):** Para botones, menús, etiquetas, garantizando claridad, modernidad y buen rendimiento en la UI.
* **Tamaños de Fuente (usando `rf` de `react-native-responsive-fontsize` para escalado automático):**
    * **Título Principal (Inicio/Fin):** `rf(5)` a `rf(7)` (5% a 7% del alto de la pantalla, escalable).
    * **Título de Nodo:** `rf(3.5)` a `rf(4.5)`.
    * **Texto Narrativo (Cuerpo):** `rf(2)` a `rf(2.4)`. Se buscará un tamaño que permita entre `50` y `70` caracteres por línea (en dispositivos de ancho `360dp`) para una lectura cómoda. El tamaño debe adaptarse para mantener la legibilidad en pantallas más grandes.
    * **Texto de Opciones/Botones:** `rf(2.1)` a `rf(2.5)`.
    * **Texto de Menú/UI Pequeño:** `rf(1.8)` a `rf(2)`.
* **Color de Fuente:** Paleta de colores cohesiva y legible, con **contraste mínimo de 4.5:1** (según WCAG) sobre el color de fondo para asegurar la accesibilidad.
* **Peso de Fuente:** Regular para el cuerpo del texto, Semibold/Bold para títulos y opciones para jerarquía visual.

### 5. **Consideraciones de Accesibilidad (Enfocadas en Pantallas Modernas):**
* **Tamaños de fuente ajustables:** Aunque `rf` ya lo hace, se considerará la compatibilidad con las configuraciones de "Tamaño de Fuente" del sistema operativo del usuario.
* **Alto Contraste:** Adherirse estrictamente a las pautas de contraste WCAG (Web Content Accessibility Guidelines) para texto e interactivos.
* **Objetivos Táctiles:** Todos los elementos interactivos (botones, enlaces) deben tener un área mínima de `48dp` x `48dp` para una fácil interacción, incluso para usuarios con dedos más grandes.
* **Modo Oscuro:** Considerar la implementación de un modo oscuro (`Dark Theme`) como opción para reducir la fatiga visual, especialmente en pantallas OLED/AMOLED de dispositivos modernos.
* **Controles de Velocidad de Texto:** Ofrecer al usuario la opción de ajustar la velocidad a la que aparece el texto en pantalla (si se usa un efecto de escritura a máquina o aparición progresiva) o la duración de las pausas en los diálogos, para adaptarse a diferentes ritmos de lectura.
* **Opciones de Vibración/Feedback Háptico:** Utilizar el feedback háptico (vibración) en momentos clave, decisiones importantes o como confirmación de interacción, para una experiencia más inmersiva y accesible.

### 6. **Paleta de Colores y Temas (Ejemplo - Adaptable y Moderna):**
* **Primario (Acción/Acento):** Colores vibrantes y saturados que guían la atención (ej. un azul profundo, un verde esmeralda o un púrpura real).
* **Secundario (Soporte):** Tonos más suaves que complementen el primario.
* **Fondo:**
    * **Claro:** Tonos sutiles de gris cálido o blanco roto (`#F5F5F5`, `#FAFAFA`).
    * **Oscuro (para Modo Oscuro):** Gris oscuro profundo (`#121212`) o negro puro (`#000000`) para aprovechar los negros perfectos de las pantallas OLED.
* **Texto:**
    * **Sobre claro:** Gris muy oscuro (`#212121`) o negro puro.
    * **Sobre oscuro:** Blanco puro o gris muy claro (`#E0E0E0`).
* **Estados:** Colores claros y distintos para éxito (verde), error (rojo), advertencia (amarillo).

---

## 📁 ORGANIZACIÓN DEL PROYECTO (Con Componentes de UI y Temas)

```
proyecto/
├── motor/                  # Código Python del Backend del Juego
│   ├── data/
│   │   ├── nodes/          # Archivos JSON de nodos narrativos (por tipo, era, etc.)
│   │   ├── npcs/           # Datos de NPCs fijos
│   │   ├── items/          # Datos de objetos
│   │   └── locations/      # Datos de ubicaciones y arcos de historia asociados
│   ├── logic/
│   │   ├── game_generator.py # Generación inicial de personaje y mundo
│   │   ├── story_engine.py   # Gestión de ramificaciones, avance narrativo
│   │   ├── character.py      # Modelo de personaje y atributos
│   │   ├── world_events.py   # Lógica de eventos aleatorios
│   │   ├── npc_manager.py    # Lógica para gestión de NPCs (estado, encuentros condicionales)
│   │   ├── location_arcs.py  # Lógica para gestionar arcos de historia por localización
│   │   └── utils.py          # Utilidades diversas
│   ├── db/                 # Base de datos local del juego
│   │   └── game.db         # Archivo de la base de datos SQLite (persistencia local)
│   └── api.py              # Implementación de la API "interna" (comunicación con RN)
│
├── app/                    # Código React Native del Frontend
│   ├── src/
│   │   ├── components/     # Componentes de UI reutilizables y responsivos (ej. CustomButton, ResponsiveText, SceneImage, DynamicText)
│   │   ├── screens/        # Pantallas principales (GameScreen, StartScreen, EndScreen)
│   │   ├── services/       # Interacción con la API de Python, AsyncStorage
│   │   │   └── game_api.ts # Gestiona llamadas al backend Python local
│   │   ├── hooks/          # Custom Hooks de React (ej. useGameData, useAudio, useResponsiveDimensions)
│   │   ├── context/        # Contextos globales (ej. GameContext, ThemeContext)
│   │   ├── themes/         # Definiciones de estilos y temas (react-native-paper, NativeBase, custom)
│   │   │   ├── lightTheme.ts
│   │   │   └── darkTheme.ts
│   │   ├── types/          # Definiciones de TypeScript para datos de juego
│   │   ├── utils/          # Utilidades para cálculo responsivo (ej. responsiveScaler.ts)
│   │   └── App.tsx         # Punto de entrada de la aplicación
│   ├── assets/
│   │   ├── images/         # Sprites e imágenes de UI (PNG, JPG, WebP en varias densidades @2x, @3x, @4x)
│   │   ├── audio/          # Archivos de música y SFX (WAV, MP3, OGG de alta calidad)
│   │   └── fonts/          # Fuentes personalizadas (TTF, OTF)
│   ├── app.json            # Configuración de Expo
│   ├── package.json        # Dependencias de React Native
│   └── tsconfig.json       # Configuración de TypeScript
│
├── README.md
├── requirements.txt        # Dependencias de Python
└── .env                    # Variables de entorno (si es necesario para el build local)
```

---

## 📄 FORMATO DE NODOS JSON (Usado por el Motor Python)

Cada archivo representa un nodo narrativo, procesado por el motor Python y enviado al frontend. Podría incluir referencias a NPCs que se deben verificar.

```json
{
  "id": "evento_inicio_pobreza",
  "type": "start",
  "location_arc": "aldea_campesina", // Nuevo: Indica el arco de historia/localización actual
  "title": "Nacimiento en la Pobreza",
  "image": "birth_poor_village.webp",
  "text": "Naciste en una aldea humilde, rodeado de hambre y enfermedades. El aire era pesado, la esperanza escasa.",
  "music": "melancholy_theme.mp3",
  "sfx": "baby_cry.wav",
  "options": [
    {
      "text": "Intentar trabajar desde joven",
      "next_node_id": "evento_trabajo_duro"
    },
    {
      "text": "Buscar educación a toda costa",
      "next_node_id": "evento_estudio"
    }
  ],
  "effects": {
    "health_change": -5,
    "money_change": 0,
    "memory_add": "Primeros años de privación."
  },
  "npcs_present": ["OldManGrumpy", "YoungMaria"] // Nuevo: NPCs que podrían estar presentes en este nodo
}
```
> **Nota:** Los campos `"image"`, `"music"`, `"sfx"` son rutas o nombres de archivo relativos a la carpeta `assets/images` o `assets/audio` del frontend. Si no existen, el frontend los omitirá o mostrará un placeholder.

---

## 🖼️ INTEGRACIÓN DE IMÁGENES Y AUDIO

* **Imágenes:**
    * Los nombres de archivo de imagen (ej. `birth_poor_village.webp`) se tomarán del campo `"image"` del JSON.
    * El frontend los cargará desde `/app/assets/images/`.
    * **Soporte de Densidad:** Las imágenes se proporcionarán en diferentes densidades (`@2x`, `@3x`, `@4x`) para asegurar nitidez en pantallas de alta densidad. Se utilizarán herramientas de optimización de imágenes para reducir el tamaño de los archivos sin comprometer la calidad visual.
    * Si una imagen no existe, se mostrará un placeholder visual genérico (un color sólido o un icono) y se registrará un error en la consola de desarrollo.
* **Audio:**
    * Los nombres de archivo de música (`music.mp3`) y efectos de sonido (`sfx.wav`) se tomarán de los campos `"music"` y `"sfx"` del JSON.
    * El frontend los cargará desde `/app/assets/audio/`.
    * Se usarán **transiciones suaves** para la música de fondo (fade-in/fade-out de 1-2 segundos) al cambiar de nodo, evitando cortes abruptos.
    * Los efectos de sonido se reproducirán de forma no bloqueante y pueden superponerse para crear una atmósfera rica.

---

## 🧪 TESTEO Y DEBUGGING

### ✅ **1. Test Unitarios del Motor (Python)**
-   **Herramienta**: `pytest`
-   **Qué se prueba**:
    -   Generación aleatoria de personajes y eventos iniciales.
    -   Validación de ramificaciones narrativas (transiciones correctas entre nodos).
    -   Lógica del sistema de edad, salud, atributos.
    -   Correcto procesamiento de eventos y objetos defectuosos.
    -   **Persistencia de datos en SQLite local:** pruebas de guardado y carga de estado del juego, NPCs, etc.
    -   **Lógica de NPCs:** Estado inicial, cambios de estado basados en interacciones, encuentros condicionales.
    -   **Lógica de Arcos de Localización:** Correcta transición entre arcos, selección de nodos basados en el arco actual.

### ✅ **2. Test Unitarios del Frontend (React Native)**
-   **Herramienta**: `Jest`, `React Native Testing Library`
-   **Qué se prueba**:
    -   Renderizado correcto de la UI (texto, opciones, imágenes, botones).
    -   Manejo de estados de la UI (carga, error, etc.).
    -   **Comunicación con el backend Python local.**
    -   Persistencia de datos con `expo-secure-store` / `AsyncStorage` (si aplica).
    -   **Validación robusta del responsividad:** Asegurar que los componentes se adaptan a diferentes tamaños de pantalla y relaciones de aspecto simuladas para asegurar que el contenido no se corte o desborde.

### ✅ **3. Test de Integración Backend-Frontend (Local)**
-   Simular el ciclo completo de comunicación entre el frontend de React Native y el backend Python ejecutándose localmente.
-   Probar el guardado y la carga de partidas completas, asegurando que el estado del juego se mantenga correctamente.
-   Verificar que todas las interacciones (decisiones, efectos) se procesen correctamente y que la base de datos local se actualice.

### ✅ **4. Test End-to-End (E2E)**
-   **Herramienta sugerida**: `Detox` o `Appium` (para la app móvil).
-   Simula el flujo completo del jugador en un emulador o dispositivo real: inicio → decisiones → finales → reinicios.
-   **Probar diferentes caminos de vida y arcos de localización** para verificar que los NPCs aparezcan y se comporten como se espera.
-   Validar la experiencia de usuario general, la coherencia narrativa y la **fluidez visual en una amplia gama de dispositivos Android modernos**.
-   **Confirmar funcionalidad offline completa.**

### ✅ **5. Pruebas Manuales y de Usabilidad**
-   Reproducción extensiva para evaluar la experiencia de usuario y la calidad auditiva final.
-   Pruebas en **dispositivos físicos reales Android** de diferentes fabricantes y generaciones recientes (ej. dispositivos con altas resoluciones y relaciones de aspecto 19:9, 20:9, 21:9) para garantizar la portabilidad y el rendimiento en el hardware real.
-   **Pruebas de Usabilidad con usuarios reales** para identificar puntos de fricción en la UI y la narrativa, así como la comprensión de la lógica de NPCs y arcos.
-   **Verificación de consumo de batería y recursos** al ejecutar el backend Python localmente.

---

## 🛠️ PASOS DE IMPLEMENTACIÓN INICIAL

### 1. Preparar el Motor en Python
-   Crear el directorio `/motor/` con la estructura de carpetas definida.
-   Definir la estructura base de los nodos JSON y crear algunos ejemplos iniciales en `/motor/data/nodes/`.
-   **Diseñar el esquema de la base de datos SQLite para NPCs y arcos de localización.**
-   Implementar la lógica del `game_generator.py` y el `story_engine.py` de forma rudimentaria.
-   **Implementar `npc_manager.py` y `location_arcs.py` con lógica básica.**
-   **Configurar la API de Python para que sea accesible desde el frontend cuando se ejecute localmente (ej. usando un mini-servidor en un hilo o exponiendo funciones a través de un puente nativo en Android).**

### 2. Preparar UI en React Native
-   Iniciar proyecto con `npx create-expo-app --template expo-template-blank-typescript`.
-   Instalar dependencias clave para UI y responsividad:
    ```bash
    npm install react-native-paper native-base expo-secure-store expo-av axios react-native-responsive-fontsize expo-font react-native-size-matters
    ```
-   Diseñar la `GameScreen` con `react-native-paper` y `NativeBase` para el texto, las opciones y el espacio para la imagen, aplicando los principios de diseño responsivo.
-   Crear el servicio `game_api.ts` para las llamadas al backend Python local.

### 3. Conectar UI con Motor
-   Implementar las llamadas desde el servicio `game_api.ts` al backend Python local. **Esto requerirá la configuración del puente nativo de Android o el mini-servidor Python embebido.**
-   Manejar los estados de carga y error en la UI, con feedback visual al usuario.

### 4. Integrar Audio e Imágenes
-   Añadir algunos assets de prueba en `/app/assets/images/` y `/app/assets/audio/`.
-   Implementar la lógica en el frontend para cargar y reproducir audio e imágenes según el nodo actual, con manejo de caché y optimización de rendimiento.

### 5. Guardar Estado del Juego
-   El backend Python gestionará la persistencia de la partida directamente en el archivo `game.db` SQLite local en el almacenamiento de la aplicación Android. El frontend solo enviará comandos de guardar/cargar al backend.

### 6. Prueba Funcional Completa
-   Ejecutar una partida completa para validar el flujo básico: inicio → decisiones → cambio de nodos → final.
-   Verificar que el guardado/carga de partida funcione correctamente de forma local.
-   **Validar el comportamiento responsivo de la UI exhaustivamente** en simuladores y dispositivos Android con diferentes resoluciones y relaciones de aspecto.
-   **Validar los encuentros con NPCs y el cambio de arcos de historia.**

---

## 📦 EXPORTACIÓN FINAL

-   Usar **EAS Build** para exportar la aplicación final para Android:
    ```bash
    eas build --platform android --profile production
    ```
-   **Configuración Crítica para la Ejecución Local de Python en Android:**
    * Se investigará y se implementará una solución como **Chaquopy** (o similar) para integrar el intérprete de Python y todo el código del `motor/` (incluyendo la DB SQLite) directamente dentro del APK/AAB. Esto permitirá que el frontend de React Native se comunique con el backend Python de forma nativa/interna.

---

## 📌 Notas Importantes

-   **El jugador no tiene sprite propio**, vive la historia "a través de sus ojos". Las imágenes son del entorno o de personajes con los que interactúa.
-   La **calidad del audio** se logrará con buenos assets de audio (música, SFX) de alta fidelidad y una reproducción fluida a través de `expo-av`.
-   **La interfaz de usuario priorizará la legibilidad, la claridad y la facilidad de navegación**, con un diseño moderno, limpio y visualmente atractivo que se adapte perfectamente a las pantallas de alta resolución.
-   El sistema notificará visualmente y por consola si un asset (imagen, audio) o un nodo narrativo no existe o no es válido, facilitando la identificación de problemas.
-   **El desafío técnico principal para este proyecto será la correcta integración del backend Python en el entorno Android nativo, manteniendo un buen rendimiento y un tamaño de aplicación razonable.**

---

## 🚀 VISIÓN Y EXPANSIÓN DEL PROYECTO

* **Diseño Modular para Expansiones:** La arquitectura del juego (especialmente la gestión de nodos JSON y la base de datos SQLite) está diseñada para permitir la adición modular de nuevas historias, arcos de localización, NPCs, eventos y objetos en futuras actualizaciones. Esto asegura la capacidad de expandir continuamente el universo del juego y la rejugabilidad a lo largo del tiempo.
* **Enfoque en el Feedback del Jugador:** A pesar de la naturaleza generativa del juego, se buscará activamente el feedback de la comunidad para refinar las ramas narrativas, equilibrar los eventos aleatorios y mejorar la experiencia general, manteniendo la visión central de la impredecibilidad.

---

### **IMPORTANTE**

Revisar siempre que las dependencias sean compatibles entres si 

### **Instrucción de Commit para el Código Generado:**

**Cada vez que genere código que deba ser integrado en el proyecto, enviaré un mensaje de commit y las instrucciones de push para su repositorio.**

```bash
git remote add origin git@github.com:casmian/AlifeIsArandomJournney.git
git branch -M main
git push -u origin main
```