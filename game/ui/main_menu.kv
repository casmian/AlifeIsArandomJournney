# Main Menu Layout - A Life Is A Random Journey
# Optimizado para APK móvil - Estilo Undertale/Deltarune/Celeste

<MainMenuScreen>:
    orientation: 'vertical'
    spacing: 0
    padding: [15, 15, 15, 15]  # Padding optimizado para móvil
    canvas.before:
        # Fondo negro profundo
        Color:
            rgba: 0, 0, 0, 1
        Rectangle:
            pos: self.pos
            size: self.size
    
    # Espaciador superior más pequeño para móvil
    Widget:
        size_hint: 1, 0.12
    
    # Título principal con mejor proporción móvil
    BoxLayout:
        size_hint: 1, 0.18
        orientation: 'vertical'
        
        Label:
            id: title_label
            text: '🏠 A LIFE\'S\nRANDOM JOURNEY'
            font_size: '18sp'  # Tamaño optimizado para móvil
            color: 1, 1, 1, 1  # Blanco puro sobre negro
            halign: 'center'
            valign: 'center'
            text_size: self.size
    
    # Subtítulo más legible en móvil
    BoxLayout:
        size_hint: 1, 0.12
        orientation: 'vertical'
        
        Label:
            id: subtitle_label
            text: '⭐ A LIFE\'S RANDOM\nJOURNEY ⭐\nRPG de Aventuras'
            font_size: '10sp'  # Tamaño apropiado para móvil
            color: 1, 1, 0.7, 1  # Amarillo dorado
            halign: 'center'
            valign: 'center'
            text_size: self.size
    
    # Espaciador medio
    Widget:
        size_hint: 1, 0.08
    
    # Área de botones principales con mejor espaciado táctil
    BoxLayout:
        size_hint: 1, 0.4
        orientation: 'vertical'
        spacing: 12  # Espaciado táctil apropiado
        padding: [30, 0, 30, 0]  # Padding lateral para móvil
        
        # Botón Comenzar Aventura - Destacado
        Button:
            id: start_button
            text: '❤️ COMENZAR\nAVENTURA'
            font_size: '12sp'  # Tamaño legible en móvil
            size_hint: 1, 0.35
            background_color: 0, 0, 0, 0  # Transparente
            color: 1, 1, 0.7, 1  # Amarillo destacado
            halign: 'center'
            valign: 'center'
            on_release: root.start_new_game()
            canvas.before:
                Color:
                    rgba: 1, 1, 0.7, 1  # Borde amarillo destacado
                Line:
                    rectangle: self.x, self.y, self.width, self.height
                    width: 2
            canvas.after:
                Color:
                    rgba: 0.1, 0.1, 0.05, 0.8  # Fondo sutil
                Rectangle:
                    pos: self.pos
                    size: self.size
        
        # Botón Configuración
        Button:
            id: config_button
            text: 'CONFIGURACIÓN'
            font_size: '11sp'
            size_hint: 1, 0.25
            background_color: 0, 0, 0, 0
            color: 0.8, 0.8, 0.8, 1  # Gris claro
            halign: 'center'
            valign: 'center'
            on_release: root.open_config()
            canvas.before:
                Color:
                    rgba: 0.8, 0.8, 0.8, 1  # Borde gris
                Line:
                    rectangle: self.x, self.y, self.width, self.height
                    width: 1
        
        # Botón Salir
        Button:
            id: exit_button
            text: 'SALIR DEL JUEGO'
            font_size: '11sp'
            size_hint: 1, 0.25
            background_color: 0, 0, 0, 0
            color: 0.8, 0.8, 0.8, 1
            halign: 'center'
            valign: 'center'
            on_release: root.exit_game()
            canvas.before:
                Color:
                    rgba: 0.8, 0.8, 0.8, 1
                Line:
                    rectangle: self.x, self.y, self.width, self.height
                    width: 1
    
    # Espaciador inferior adaptable
    Widget:
        size_hint: 1, 0.1 